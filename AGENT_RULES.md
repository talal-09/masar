## الهدف العام
يجب على الـ AI Agent الالتزام ببنية المشروع الحالية وعدم تغيير طريقة البناء أو التنظيم إلا عند الحاجة الواضحة والمبررة.
---
## 1. قواعد العمل في Backend
يجب العمل على الملفات الأصلية الموجودة داخل كل تطبيق Django فقط، وهي:
- `admin.py`
- `models.py`
- `urls.py`
- `views.py`
يمنع إنشاء ملفات جديدة إلا عند الحاجة الفعلية والضرورية، مثل:
- الحاجة إلى `forms.py`
- الحاجة إلى `services.py`
- الحاجة إلى `utils.py`
ولا يتم إنشاء أي ملف جديد إلا إذا كان ذلك يحسن التنظيم أو يمنع تكرار الكود أو يعالج مشكلة حقيقية.
---
## 2. عدم تغيير هيكلة المشروع
يمنع على الـ AI Agent القيام بأي من التالي:
- تغيير بنية المشروع الحالية.
- نقل الملفات من أماكنها.
- حذف ملفات موجودة.
- إنشاء مجلدات جديدة بدون حاجة.
- تغيير أسماء التطبيقات.
- تغيير أسماء الجداول أو الحقول بدون طلب صريح.
- إعادة بناء المشروع من الصفر.
المطلوب دائمًا هو:
تعديل الكود الحالي وتحسينه بنفس أسلوب المشروع الحالي.
---
## 3. قواعد القوالب Templates
القوالب يجب أن تبقى داخل مجلد القوالب الرئيسي الموجود في جذر المشروع.
البنية المعتمدة:
```txt
templates/
    app_name/
        template_file.html
````
مثال:
```txt
templates/
    products/
        product_list.html
        product_detail.html
    orders/
        order_list.html
        order_detail.html
```
كل تطبيق له مجلد باسمه داخل مجلد `templates` الرئيسي.
ممنوع:
* إنشاء مجلد `templates` داخل التطبيق نفسه.
* وضع القوالب بشكل عشوائي.
* تغيير أسماء القوالب الحالية بدون سبب.
* نقل القوالب من مكانها الحالي.
---
## 4. آلية كتابة الكود
يجب أن تكون آلية كتابة الكود والبناء بنفس الطريقة الحالية الموجودة في المشروع.
على الـ AI Agent قبل التعديل أن يفهم:
* أسلوب كتابة views الحالي.
* طريقة تعريف urls الحالية.
* طريقة بناء models الحالية.
* طريقة ربط templates الحالية.
* طريقة أسماء المتغيرات والدوال.
* طريقة المشروع في التعامل مع المستخدمين والصلاحيات.
ثم يقوم بالتعديل بنفس النمط.
---
## 5. قواعد الأمان Security
الأمان أولوية عالية جدًا.
يجب الالتزام بالتالي:
* استخدام Django ORM بدل SQL مباشر.
* عدم كتابة Raw SQL إلا للضرورة القصوى ومع شرح السبب.
* حماية النماذج باستخدام CSRF.
* التحقق من مدخلات المستخدم.
* عدم الثقة بأي بيانات تأتي من المستخدم.
* منع XSS بعدم استخدام `safe` إلا للضرورة.
* عدم طباعة الأخطاء الحساسة للمستخدم.
* عدم كشف `SECRET_KEY` أو بيانات الاتصال أو مفاتيح API.
* عدم وضع كلمات مرور أو مفاتيح داخل الكود.
* استخدام الصلاحيات المناسبة قبل عرض أو تعديل البيانات.
* التأكد أن المستخدم لا يرى إلا بياناته المصرح له بها.
* استخدام `login_required` عند الحاجة.
* استخدام `permission_required` أو فحص الصلاحيات عند الحاجة.
* عدم تعطيل الحماية الافتراضية في Django.
---
## 6. قواعد الكود النظيف Clean Code
يجب أن يكون الكود:
* واضحًا.
* بسيطًا.
* قابلًا للصيانة.
* غير مكرر.
* منظمًا.
* سهل القراءة.
يجب تجنب:
* التكرار غير الضروري.
* الدوال الطويلة جدًا.
* التعقيد الزائد.
* كتابة منطق كبير جدًا داخل template.
* أسماء مبهمة للمتغيرات.
* حلول مؤقتة تؤثر على المشروع لاحقًا.
---
## 7. قواعد الأداء Performance
يجب مراعاة الأداء عند كتابة الكود.
التزم بالتالي:
* استخدام `select_related` عند العلاقات ForeignKey.
* استخدام `prefetch_related` عند العلاقات ManyToMany أو reverse relations.
* عدم تنفيذ queries داخل loops قدر الإمكان.
* عدم تحميل بيانات غير مستخدمة.
* استخدام pagination عند عرض قوائم كبيرة.
* تقليل العمليات الثقيلة داخل views.
* عدم تنفيذ عمليات طويلة داخل request مباشرة إذا كانت تحتاج Celery.
---
## 8. قواعد الأخطاء Error Handling
يجب التعامل مع الأخطاء بطريقة آمنة وواضحة.
التزم بالتالي:
* استخدام `try/except` عند الحاجة فقط.
* عدم إخفاء الأخطاء بدون سبب.
* عدم عرض تفاصيل تقنية حساسة للمستخدم.
* عرض رسائل مفهومة للمستخدم.
* تسجيل الأخطاء عند الحاجة.
* عدم استخدام `except Exception` بشكل عشوائي إلا مع سبب واضح.
---
## 9. قواعد قاعدة البيانات Models
عند تعديل `models.py`:
* لا تحذف حقولًا موجودة إلا بطلب صريح.
* لا تغير أسماء الحقول إلا بطلب صريح.
* لا تغير العلاقات إلا عند الحاجة الواضحة.
* حافظ على توافق البيانات الحالية.
* أضف `verbose_name` و `verbose_name_plural` عند الحاجة.
* استخدم `__str__` واضح لكل Model.
* راعِ `null` و `blank` بشكل صحيح.
* لا تضف migrations يدويًا إلا عند الحاجة.
بعد أي تعديل في `models.py` يجب تذكير المستخدم بتنفيذ:
```bash
python manage.py makemigrations
python manage.py migrate
```
---
## 10. قواعد URLs
عند تعديل `urls.py`:
* التزم بنفس نمط المشروع الحالي.
* استخدم أسماء واضحة للمسارات.
* ضع `name` لكل path.
* لا تغير routes الحالية إلا عند الحاجة.
* لا تكسر روابط موجودة.
* لا تنشئ routes مكررة.
---
## 11. قواعد Views
عند تعديل `views.py`:
* التزم بنفس طريقة المشروع الحالية.
* استخدم function-based views أو class-based views حسب الموجود حاليًا.
* لا تخلط الأنماط بدون سبب.
* تحقق من صلاحيات المستخدم.
* استخدم `get_object_or_404` عند الحاجة.
* استخدم redirects واضحة.
* استخدم messages عند الحاجة.
* لا تضع منطق ضخم ومعقد داخل view إذا كان يمكن تبسيطه.
---
## 12. قواعد Admin
عند تعديل `admin.py`:
* سجل النماذج بطريقة منظمة.
* استخدم `list_display` عند الحاجة.
* استخدم `search_fields` عند الحاجة.
* استخدم `list_filter` عند الحاجة.
* لا تعرض بيانات حساسة في لوحة التحكم.
* اجعل لوحة التحكم سهلة الاستخدام.
---
## 13. قواعد Templates
عند تعديل ملفات HTML:
* التزم بنفس التصميم الحالي.
* لا تغير الهوية البصرية بدون طلب.
* لا تحذف أقسام موجودة بدون سبب.
* استخدم `{% csrf_token %}` داخل النماذج.
* استخدم `{% url %}` بدل كتابة الروابط يدويًا.
* استخدم `{% static %}` للملفات الثابتة.
* لا تضع منطق برمجي معقد داخل القالب.
* حافظ على دعم اللغة العربية واتجاه RTL إذا كان موجودًا.
---
## 14. قواعد Static Files
يمنع إنشاء أو نقل ملفات CSS و JS إلا عند الحاجة.
عند التعديل:
* لا تكسر التصميم الحالي.
* لا تكرر CSS بدون حاجة.
* لا تضف مكتبات خارجية بدون طلب.
* لا تستخدم روابط CDN عشوائية في الإنتاج بدون ضرورة.
---
## 15. قواعد الاعتمادات Packages
يمنع إضافة مكتبات جديدة إلا عند الحاجة الفعلية.
قبل إضافة أي مكتبة يجب توضيح:
* لماذا نحتاجها؟
* هل يوجد حل موجود داخل Django؟
* هل المكتبة آمنة ومحدثة؟
* هل ستؤثر على الأداء أو النشر؟
---
## 16. قواعد الإنتاج Production
يجب عدم كتابة كود يسبب مشاكل في الإنتاج.
ممنوع:
* ترك `DEBUG=True` كحل دائم.
* طباعة بيانات حساسة.
* تعطيل CSRF.
* تعطيل Authentication.
* تجاهل الصلاحيات.
* استخدام حلول مؤقتة غير آمنة.
* كتابة أسرار داخل الكود.
---
## 17. قواعد التعديلات
قبل أي تعديل يجب على الـ AI Agent:
1. قراءة الكود الحالي.
2. فهم البنية.
3. تحديد أقل تعديل ممكن.
4. الحفاظ على السلوك الحالي.
5. تنفيذ المطلوب فقط.
6. عدم إضافة أشياء غير مطلوبة.
---
## 18. قواعد المخرجات
عند الرد على المستخدم يجب توضيح:
* ما الملفات التي تم تعديلها.
* لماذا تم تعديلها.
* هل يحتاج تنفيذ migrations أم لا.
* هل يوجد أمر تشغيل أو اختبار.
* هل توجد ملاحظات أمنية مهمة.
---
## 19. ممنوعات صارمة
يمنع تمامًا:
* إعادة هيكلة المشروع بدون طلب.
* إنشاء تطبيقات جديدة بدون طلب.
* إنشاء ملفات كثيرة بدون حاجة.
* حذف كود يعمل.
* تغيير أسلوب المشروع.
* إضافة REST API إذا لم يطلب المستخدم.
* إضافة React أو Vue أو أي Framework خارجي بدون طلب.
* تغيير قاعدة البيانات بدون طلب.
* تغيير إعدادات الإنتاج بدون فهم كامل.
* تجاهل الأمان.
---
## 20. القاعدة الأساسية
الأصل دائمًا:
> عدّل الموجود، ولا تبني من جديد.
> التزم بهيكل المشروع الحالي.
> الأمان أولًا.
> الكود النظيف ثانيًا.
> الأداء والاستقرار دائمًا.
````